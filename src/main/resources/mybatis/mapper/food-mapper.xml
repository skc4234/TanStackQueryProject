<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.mapper.FoodMapper">
	<select id="foodListData" resultType="com.sist.web.vo.FoodVO" parameterType="hashmap">
		SELECT no,poster,name,score,type,theme
		FROM food
		<choose>
			<when test="search!=null and search!=''">
				WHERE address LIKE '%'||#{search}||'%'
			</when>
		</choose>
		ORDER BY no ASC
		OFFSET #{start} ROWS FETCH NEXT 12 ROWS ONLY
	</select>
	<!-- trim / if / choose~when 
		 where / foreach
	-->
	<select id="foodListRowCount" resultType="int" parameterType="string">
		SELECT COUNT(*)
		FROM food
		<choose>
			<when test="search!=null and search!=''">
				WHERE address LIKE '%'||#{search}||'%'
			</when>
		</choose>		
	</select>
</mapper>